let bcrypt;_c64‍.x([["hashPassword",()=>hashPassword],["comaprePassword",()=>comaprePassword]]);_c64‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);

//to hash password
       const hashPassword = (password) => {
  return new Promise((resolve, reject) => {
    bcrypt.genSalt(10, (err, salt) => {
      if (err) {
        reject(err);
      }
      bcrypt.hash(password, salt, (err, hash) => {
        if (err) reject(err);
        resolve(hash);
      });
    });
  });
};

//compare password
       const comaprePassword = (password, hashed) => {
  return bcrypt.compare(password, hashed);
};
